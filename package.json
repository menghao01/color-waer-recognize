{
  "name": "color-wear-recognize",
  "version": "1.0.0",
  "description": "Color matching and outfit recommendation system",
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": "^20.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}